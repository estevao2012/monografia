$(".fade-loading").show()
var count = <%= @locals.count %>;
<% @locals.each do |single| %>
  var item = createVector("#<%= random_color %>", <%= raw single.json_geom.to_json %>);
  map.addLayer(item);
  count--;
  if(count == 0) $(".fade-loading").hide();
<% end %>