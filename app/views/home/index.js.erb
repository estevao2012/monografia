var count = <%= @locals.count %>;
<% @locals.each do |single| %>
  var item = createVector("#<%= random_color %>", <%= raw single.json_geom %>, "<%= single.id %>");
  if(item !== undefined) map.addLayer(item);

  count--;
  if(count == 0) $(".fade-loading").hide();
<% end %>