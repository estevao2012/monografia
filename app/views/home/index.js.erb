<% @locals.each do |single| %>
  var item = createVector("#<%= random_color %>", <%= raw single.json_geom.to_json %>, "<%= single.id %>");
  console.log(item);
  if(item !== undefined)
  	map.addLayer(item);
<% end %>