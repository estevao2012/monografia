
<div class="alert alert-info hide-success" role="alert">Estamos enviando, aguarde!</div>

<p> Clique no mapa a localização aproximada da marcação. Evite clicar sobre a linha da rodovia , clique na lateral para determinar o ponto por proximidade. </p>
<%= form_for([@rodovia, @via_caracteristic], url: {action: "create"} , remote: true, method: :post) do |f| %>
  <% if @via_caracteristic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@via_caracteristic.errors.count, "error") %> prohibited this via_caracteristic from being saved:</h2>
      <ul>
      <% @via_caracteristic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
  <% end %>

    <div class="form-group">
      <!-- <label for="exampleInputEmail1">Nome</label> -->
      <%= f.text_field :name, placeholder: "Nome", class: "form-control" %>
    </div>
    <div class="form-group">
      <!-- <label for="exapleInputEmail1">Nome</label> -->
      <%= f.text_area :description, placeholder: "Descrição", class: "form-control" %>
    </div>
    <div class="form-group">
      <!-- <label for="exampleInputEmail1">Nome</label> -->
      <%= select_tag "via_caracteristic[via_caracteristic_categorys_id]", options_from_collection_for_select(ViaCaracteristicCategory.all, :id, :name), {class: "form-control"} %>
    </div>
    
    <div class="form-group">
      Coord X Inicio: <%= f.text_field :coord_x, class: "coord-x-form form-control", readonly: true %>
      Coord Y Inicio: <%= f.text_field :coord_y, class: "coord-y-form form-control", readonly: true %>   
    </div>  
    <div class="form-group">
      Coord X Final: <%= f.text_field :coord_x_b, class: "coord-xb-form form-control", readonly: true %>
      Coord Y Final: <%= f.text_field :coord_y_b, class: "coord-yb-form form-control", readonly: true %>   
    </div>   
  </div>

  <div class="actions">
    <%= f.button type: :submit, class: "btn btn-create-ref" do %>
      Cadastrar
    <% end%>

    <a href="#" class="btn close-this">Cancelar</a>
  </div>
<% end %>
